int g = 1;

int fib(int n) {
    if (n <= 1) {
        return n + g - g;
    }
    return fib(n - 1) + fib(n - 2);
}

int main() {
    int sum = 0;
    int i = 0;
    while (i < 9) {
        sum = sum + fib(12);
        i = i + 1;
    }
    return sum % 256;
}
